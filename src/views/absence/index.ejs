<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="referrer" content="strict-origin" />
    <title>opentime - Absenties</title>

    <link rel="stylesheet" href="/css/tailwind.css">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
    <script defer src="/js/preline.js"></script>
</head>
<body>
    <% 
        let last_name = entree_user.sn;
        let last_name_split = last_name.split(", ");
        if(last_name_split.length > 1) {
            last_name = last_name_split[1] + " " + last_name_split[0];
        }
    %>
    <%- include("../partials/header.ejs", { entree_user: entree_user, last_name: last_name }) %>
    <%- include("../partials/sidebar.ejs", { entree_user: entree_user, last_name: last_name, page: "Absenties" }) %>
    <div class="w-full pt-10 px-4 sm:px-6 md:px-8 lg:pl-72">
        <header>
            <h1 class="block text-2xl font-bold text-gray-800 sm:text-3xl dark:text-white">Absenties</h1>
        </header>
        <section>
            <br>
            <p>Je hebt op dit moment geen absentiemeldingen</p>
        </section>
    </div>
</body>
</html>